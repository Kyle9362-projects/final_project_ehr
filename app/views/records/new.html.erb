<!-- Validation failure messages -->
<% if @record.errors.any? %>
  <% @record.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Record</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_record" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for prescription_id -->
      <div class="form-group">
        <label for="prescription_id" class="control-label">
          Prescription
        </label>

        <%= select_tag(:prescription_id, options_from_collection_for_select(Prescription.all, :id, :doctor_id, @record.prescription_id), :class => "form-control") %>
      </div>

      <button class="btn btn-success">
        Create Record
      </button>
      or
      <a href="/records">Cancel</a>
    </form>
  </div>
</div>
